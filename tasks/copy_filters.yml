- name: copy filters
  ansible.builtin.copy:
    src: "{{ fail2ban_filterd_path }}"
    dest: /etc/fail2ban/filter.d/
    owner: root
    group: root
    mode: '0644'
  notify: restart fail2ban
  tags:
    - configuration
    - fail2ban
    - fail2ban-filters
